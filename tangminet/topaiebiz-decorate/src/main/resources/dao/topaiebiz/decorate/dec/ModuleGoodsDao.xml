<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.topaiebiz.dec.dao.ModuleGoodsDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, moduleId, goodsId, sortNo, memo, creatorId, createdTime, deletedFlag, version
    </sql>

	<!-- 逻辑删除单条区域数据  -->
	<!--<delete id="deleteModuleGoods">
		UPDATE t_dec_module_goods SET deletedFlag = 1
		WHERE id =
		#{moduleGoodsId}
	</delete>-->

	<!--<update id="updateDelFlagByModuleId" parameterType="java.util.Map">
		UPDATE t_dec_module_goods SET
		deletedFlag =#{deletedFlag}
		WHERE
		moduleId = #{moduleId}
	</update>

	<select id="selectByModuleId" resultType="" parameterType="java.lang.Long">
		SELECT
		<include refid="Base_Column_List"/>
		FROM t_dec_module_goods
		GROUP BY moduleId = #{moduleId}
	</select>-->
</mapper>
