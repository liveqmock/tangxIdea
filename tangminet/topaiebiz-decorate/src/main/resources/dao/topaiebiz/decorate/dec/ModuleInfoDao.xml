<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.topaiebiz.dec.dao.ModuleInfoDao">


    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, moduleId, image, jumpType, jumpValue, sortNo, memo, creatorId, createdTime, deletedFlag, version
    </sql>

	<!-- 逻辑删除单条区域数据  -->
	<!--<delete id="deleteModuleInfo">
		UPDATE t_dec_module_info SET deletedFlag = 1
		WHERE id =
		#{moduleInfoId}
	</delete>-->

 <!--   <select id="selectByModuleId" resultType="com.topaiebiz.dec.entity.ModuleGoodsEntity" parameterType="java.lang.Long">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_dec_module_info WHERE moduleId = #{moduleId}
    </select>-->
</mapper>
